package helpers

import "blood-type-compatibility/models"

func CompatibleDonors(recipient models.User) []models.User {
	// Given a recipient, find who they can receive from
	return nil
}

func CompatibleRecipients(donor models.User) []models.User {
	// Given a donor, find who they can donate to
	return nil
}
